import React from "react";

const Action = () => {
    return (
        <div>
            <button className="agree">Accepter</button>
            <button className="denied">Refuser</button>
        </div>
    );
};

export default Action;
